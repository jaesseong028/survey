@model System.Tuple<object, object, object>
@{
    ViewData["Title"] = "Home Page";
}

@section Scripts {
<environment include="Development">
    <script src="~/js/vue.min.js"></script>
</environment>
<environment include="Production">    
    <script src="~/js/vue.min.js"></script>
</environment>
<script src="~/js/axios.min.js"></script>
}

<div id="container"> 
    <table class="table table-bordered text-center">
        <tr>
            <td class="info">제목</td>
            <td><input type="text" class="form-control" v-model="searchData.title"></td>
            <td class="info">상태</td>
            <td>
                <select class="form-control" v-model="searchData.approveStatus">
                    <option value=1>승인</option>
                    <option value=0>미승인</option>
                </select>
            </td>
        </tr>
        <tr>
            <td class="info">서비스명</td>
            <td>
                유비케어 설문
            </td>
            <td class="info" >설문기간</td>
            <td><input  type="date" class="form-control" v-model="searchData.startDate">~<input  type="date" class="form-control" v-model="searchData.endDate"></td>
        </tr>
    </table>
    <div style="margin-bottom:15px"><button class="btn btn-success">설문등록</button> <button class="btn btn-info pull-right" v-on:click=search>검색</button></div>
    <table class="table table-bordered table-hover">
    <tr class="info">
        <th>NO</th>
        <th>설문제목</th>
        <th>기간</th>
        <th>상태</th>
        <th>참여인원</th>
        <th>삭제</th>
    </tr>
    <tr v-for="s in surveys">
        <td>1</td>
        <td>{{s.title}}</td>
        <td>{{s.startDateAndEndDate}}</td>
        <td>{{s.approveStatusStr}}</td>
        <td>{{s.limitPersonsStr}}</td>
        <td><button class="btn btn-danger" v-on:click="deletesurvey(s._id)" >삭제</button> </td>
    </tr>     
    </table>
    <center>
    <ul class="pagination text-center">
        <li v-if="pager.First != null"><a v-bind:href="pager.First"><<</a></li>
        <li v-if="pager.Prev != null"><a v-bind:href="pager.Prev"><</a></li>
        <li v-for="page in pager.Pages"><a v-bind:href="page">{{page}}</a></li>
        <li v-if="pager.Next != null"><a v-bind:href="pager.Next">></a></li>
        <li v-if="pager.Last != null"><a v-bind:href="pager.Last">>></a></li>
    </ul>
    </center>
    {{surveys}}
</div>

<script>
var vue = new Vue({
    el: '#container',
    data: {
        surveys : @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(this.Model.Item1)),
        searchData : @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(this.Model.Item3)),
        pager : @Html.Raw(Newtonsoft.Json.JsonConvert.SerializeObject(this.Model.Item2))
    },
    created : function(){
        
        
    }, 
    methods : {
        search : function(){
            var params = $.param(this.searchData);
            //console.log(params);
            var actionUrl = '/?' + params;
            location.href = actionUrl;
        },
        createActionUrl : function(pid){
            
        },
        deletesurvey : function(_id){
        console.log(_id);
        //headers = {'Content-Type': 'application/json; charset=UTF-8'};
        axios.post('/api/UBSurvey/delete', {id : _id})
        .then(function (response) {
            vue.selectSurvey();
        })
        .catch(function (error) {
            console.log(error);
        });
        }
    }
})
</script>